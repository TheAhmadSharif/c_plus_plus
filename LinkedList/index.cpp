// Following is the example, which will produce correct size of various data types on your computer.
  
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
  

struct Node {
    int data;
    Node *link;
}

Typeof Node* nodePtr;
int main()  {
   nodePtr head;
   head = headNode;
   headNode -> data = 20;
   headNode -> link = NULL;

   cout << head->data << "\n";
      
    return 0;
}
